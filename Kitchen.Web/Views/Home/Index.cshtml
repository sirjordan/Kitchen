@{
    ViewData["Title"] = "Ресторант \"Кухня\"";
    @model IEnumerable<DishViewModel>;
}

<div class="text-center">
    <h1 class="display-4">Добре дошли в ресторант "Кухня"</h1>
</div>

<div>
    @{
        foreach (var dish in Model)
        {
            await Html.RenderPartialAsync("~/Views/Menu/_DishPartial.cshtml", dish);
        }
    }
</div>